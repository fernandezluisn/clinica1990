import { TurnosCuarentaPipe } from './turnos-cuarenta.pipe';

describe('TurnosCuarentaPipe', () => {
  it('create an instance', () => {
    const pipe = new TurnosCuarentaPipe();
    expect(pipe).toBeTruthy();
  });
});
